# EnovaUI Design Tokens - GitHub Copilot Context

이 파일은 GitHub Copilot이 EnovaUI 디자인 토큰을 더 잘 이해하고 추천할 수 있도록 도와줍니다.

## 🎨 Design Token 사용 가이드

### Available Packages
- **core-tokens**: 기본 primitive 토큰들 (색상, 간격, 타이포그래피)
- **webos-tokens**: WebOS 플랫폼용 semantic 토큰들
- **web-tokens**: 웹 플랫폼용 semantic 토큰들
- **mobile-tokens**: 모바일 플랫폼용 semantic 토큰들

### 토큰 명명 규칙
- CSS에서는 점(.)을 대시(-)로 변경: `primitive.color.white` → `--primitive-color-white`
- 항상 `var()` 함수 사용: `var(--token-name)`
- Semantic 토큰을 primitive 토큰보다 우선적으로 사용

### 자주 사용되는 토큰들

#### 색상 토큰
```css
/* 기본 색상 */
color: var(--primitive-color-white);           /* #ffffff */
color: var(--primitive-color-black);           /* #000000 */

/* 표면 색상 */
background-color: var(--semantic-color-surface-main);
background-color: var(--semantic-color-surface-button-primary);
background-color: var(--semantic-color-surface-button-secondary);

/* 텍스트 색상 */
color: var(--semantic-color-on-surface-main);
color: var(--semantic-color-on-surface-sub);
```

#### 간격 토큰
```css
/* 간격 (4px 단위) */
margin: var(--primitive-spacing-1);     /* 4px */
margin: var(--primitive-spacing-2);     /* 8px */
margin: var(--primitive-spacing-4);     /* 16px */
margin: var(--primitive-spacing-6);     /* 24px */
padding: var(--primitive-spacing-8);    /* 32px */
```

#### 타이포그래피 토큰
```css
/* 폰트 크기 */
font-size: var(--primitive-fontsize-12);   /* 12px */
font-size: var(--primitive-fontsize-14);   /* 14px */
font-size: var(--primitive-fontsize-16);   /* 16px */
font-size: var(--primitive-fontsize-18);   /* 18px */

/* 폰트 두께 */
font-weight: var(--primitive-fontweight-regular);    /* 400 */
font-weight: var(--primitive-fontweight-medium);     /* 500 */
font-weight: var(--primitive-fontweight-semibold);   /* 600 */
font-weight: var(--primitive-fontweight-bold);       /* 700 */
```

### 컴포넌트별 추천 토큰

#### Primary Button
```css
.primary-button {
  background-color: var(--semantic-color-surface-button-primary);
  color: var(--semantic-color-on-surface-main-selected);
  padding: var(--primitive-spacing-3) var(--primitive-spacing-6);
  font-size: var(--primitive-fontsize-16);
  font-weight: var(--primitive-fontweight-medium);
  border-radius: var(--primitive-radius-4);
}

.primary-button:hover {
  background-color: var(--semantic-color-surface-button-primary-hover);
}

.primary-button:active {
  background-color: var(--semantic-color-surface-button-primary-pressed);
}
```

#### Card Component
```css
.card {
  background-color: var(--semantic-color-surface-main);
  border: 1px solid var(--semantic-color-stroke-main);
  border-radius: var(--primitive-radius-8);
  padding: var(--primitive-spacing-6);
  box-shadow: var(--primitive-elevation-1);
}

.card-title {
  color: var(--semantic-color-on-surface-main);
  font-size: var(--primitive-fontsize-18);
  font-weight: var(--primitive-fontweight-semibold);
  margin-bottom: var(--primitive-spacing-2);
}

.card-content {
  color: var(--semantic-color-on-surface-sub);
  font-size: var(--primitive-fontsize-14);
  line-height: var(--primitive-lineheight-normal);
}
```

#### Input Field
```css
.input-field {
  background-color: var(--semantic-color-surface-main);
  border: 1px solid var(--semantic-color-stroke-main);
  color: var(--semantic-color-on-surface-main);
  padding: var(--primitive-spacing-3) var(--primitive-spacing-4);
  font-size: var(--primitive-fontsize-16);
  border-radius: var(--primitive-radius-4);
}

.input-field:focus {
  border-color: var(--semantic-color-stroke-main-focused);
  outline: none;
}

.input-field:disabled {
  background-color: var(--semantic-color-surface-text-field-disabled);
  color: var(--semantic-color-on-surface-sub);
}
```

### 플랫폼별 토큰 사용

#### WebOS Platform
```css
.webos-component {
  background-color: var(--webos-semantic-color-surface-main);
  color: var(--webos-semantic-color-on-surface-main);
}

/* 다크 모드 지원 */
@media (prefers-color-scheme: dark) {
  .webos-component {
    background-color: var(--webos-dark-semantic-color-surface-main);
    color: var(--webos-dark-semantic-color-on-surface-main);
  }
}
```

#### Web Platform
```css
.web-component {
  background-color: var(--web-semantic-color-surface-main);
  color: var(--web-semantic-color-on-surface-main);
}
```

#### Mobile Platform
```css
.mobile-component {
  background-color: var(--mobile-semantic-color-surface-main);
  color: var(--mobile-semantic-color-on-surface-main);
  padding: var(--mobile-semantic-spacing-container);
}
```

### 반응형 디자인 패턴

```css
/* 모바일 우선 */
.responsive-component {
  /* 모바일 토큰 */
  padding: var(--mobile-semantic-spacing-container);
  font-size: var(--primitive-fontsize-14);
}

/* 태블릿 */
@media (min-width: 768px) {
  .responsive-component {
    padding: var(--primitive-spacing-6);
    font-size: var(--primitive-fontsize-16);
  }
}

/* 데스크톱 */
@media (min-width: 1024px) {
  .responsive-component {
    /* 웹 토큰 */
    padding: var(--web-semantic-spacing-container);
    max-width: var(--web-semantic-size-container-max);
  }
}
```

### 접근성 고려사항

```css
/* 고대비 모드 지원 */
@media (prefers-contrast: high) {
  .accessible-component {
    border-width: 2px;
    border-color: var(--semantic-color-stroke-main-focused);
  }
}

/* 모션 감소 모드 */
@media (prefers-reduced-motion: reduce) {
  .animated-component {
    transition: none;
  }
}
```

## 🤖 Copilot 사용 팁

1. **토큰 검색**: "button primary color token" 또는 "spacing between elements" 같은 자연어로 질문
2. **컴포넌트 생성**: "Create a card component using EnovaUI tokens"
3. **플랫폼 적응**: "Convert this web component to use WebOS tokens"
4. **다크 모드**: "Add dark mode support using semantic tokens"

이 컨텍스트를 참고하여 GitHub Copilot이 EnovaUI 디자인 토큰을 활용한 더 나은 코드를 제안할 수 있습니다.
